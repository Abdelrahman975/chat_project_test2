import 'package:flutter/material.dart';

class DomePage1 extends StatelessWidget {
  const DomePage1({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Dome'),
      ),
    );
  }
}
